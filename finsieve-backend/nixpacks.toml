# Force Nixpacks/Railpack to treat this as Node (22 required by yahoo-finance2)
[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install --omit=dev"]

[start]
cmd = "node src/server.js"
